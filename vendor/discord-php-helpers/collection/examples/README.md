# Collection examples

This folder contains a small usage example for the `Discord\Helpers\Collection` class.

- `collection_usage.php` — runnable PHP script demonstrating common methods: construction, `from`, `for`, `push`, `pushItem`, `get`, `pull`, `shift`, `search`, `find`, `map`, `filter`-style operations, class-restricted collections, array access (`offsetGet`/`offsetSet`), `merge`, `unique`, `fill`, `walk`, `reduce`, `serialize`/`unserialize`, `sort`, `diff`, `intersect`, and more.

To run the example (from the repository root):

```bash
php examples/collection_usage.php
```

The script uses the project's `vendor/autoload.php` generated by Composer.
